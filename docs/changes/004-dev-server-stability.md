# 004 Dev Server Stability

## Summary
개발 서버 기본 바인딩을 `0.0.0.0`에서 `127.0.0.1`로 변경하고, 호스트/포트는 환경변수로 오버라이드 가능하게 조정했다.

## Scope
- `apps/gallery-web/vite.config.mjs`

## Why
- 권한 제한이 있는 실행 환경에서 `0.0.0.0` 바인딩이 실패할 수 있음.
- 기본값을 루프백으로 두고 필요 시 `VITE_HOST=0.0.0.0`로 확장 가능하도록 해 개발 안정성 확보.
